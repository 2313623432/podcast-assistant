# 播客助手 v1.0.0

这是一个可以直接运行的 Windows 单文件版发布包。

## 下载

在本 Release 的 Assets 中下载：

```text
播客助手-单文件.exe
```

下载后双击即可使用，不需要安装 Python，不需要安装 ffmpeg，也不需要保留额外文件夹。

## 软件介绍

播客助手可以把中文长文案快速变成口播音频，并同步生成 SRT 字幕。它适合做知识科普、养生讲解、书籍拆解、短视频口播、课程音频等内容生产场景。

你只需要准备一段文案和自己的火山引擎「豆包语音」App ID / Access Token，就可以在浏览器页面里完成音频生成。

## 主要功能

- 单人模式：整段文案由一个音色播讲
- 双人模式：按角色名匹配不同音色，适合访谈或对话内容
- 长文案自动切段：避免 TTS 单次请求过长
- 自动拼接 MP3：多段合成后输出为一个完整音频
- 自动生成 SRT：方便导入剪映、Premiere、CapCut 等工具
- 可调语速、音高、音量
- 可调字幕每条字数和换行长度
- 内置 ffmpeg/ffprobe，单文件版无需额外配置

## 使用方法

1. 下载 `播客助手-单文件.exe`
2. 双击运行
3. 等待浏览器自动打开页面
4. 在左侧栏输入火山引擎「豆包语音」App ID 和 Access Token
5. 选择单人或双人模式
6. 输入音色 ID 和文案
7. 点击“生成音频与字幕”
8. 下载 MP3 和 SRT

## 注意事项

- 首次启动会比普通软件慢一些，因为单文件 EXE 需要先解压内置运行环境。
- 运行时出现的黑色命令窗口不要关闭，关闭后软件也会停止。
- 火山引擎语音合成会消耗你自己的账号额度。
- 凭证建议在侧边栏手动输入，不要把真实 Token 写进公开仓库。

## 已验证

- Windows 11
- 单文件 EXE 可独立启动
- 内置 ffmpeg/ffprobe 可用于音频处理
- Streamlit 页面可正常打开
